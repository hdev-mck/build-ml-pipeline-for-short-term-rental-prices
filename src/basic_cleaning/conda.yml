name: basic_cleaning
channels:
  - conda-forge
  - defaults
dependencies:
  - pip=20.3.3
  - pandas=1.2.3
  - pip:
      - wandb==0.10.31
      - numpy==1.23.4 #to fix the np.object error
      - grpcio-status==1.33.2
      - protobuf==3.19.6  #fixes proto package issues
